image: microsoft/dotnet:3.0.100

stages:
  - build

before_script:
  - "dotnet restore"

build:
  artifacts:
    name: "Binaries"

    paths:
      - "Ssyp.JsonDBEngine/bin/Release"

  stage: build

  script:
    - "dotnet build -c Release"
